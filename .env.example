OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
OPENAI_JUDGE_MODEL=gpt-5.4

JIRA_URL=https://your-domain.atlassian.net
JIRA_USER_EMAIL=
JIRA_API_TOKEN=
JIRA_PROJECT_KEY=KAN

ARIZE_SPACE_ID=
ARIZE_API_KEY=
ARIZE_PROJECT_NAME=drone-show-manager

# Optional: protect the deployed web UI behind HTTP Basic Auth. Leave SITE_PASSWORD
# unset for local development — the site stays open when no password is configured.
# Set both in Replit Secrets on the deployed instance.
SITE_USERNAME=adhoc
SITE_PASSWORD=
